name: Workflow Push Main

on:
  push:
    branches:
      - main

jobs:
  prueba:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar mensaje
        run: echo "Se detectó un push en la rama main"
